# Database connection string
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:password@localhost:5432/enter-the-nest?schema=public"

# JWT secret key for authentication (use a strong, random string in production)
JWT_SECRET="your_super_secret_jwt_key_change_this_in_production"

# Application port (default: 3000)
PORT=3000